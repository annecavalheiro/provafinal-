#include <stdio.h>

int main() {
    int a, b, resto;

    printf("Digite dois numeros: ");
    scanf("%d %d", &a, &b);

    int x = a, y = b;
    while (y != 0) {
        resto = x % y;
        x = y;
        y = resto;
    }

    printf("MDC de %d e %d = %d\n", a, b, x);
    return 0;
}
